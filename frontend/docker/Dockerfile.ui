# =======================================
# Stage 1: Build the React App (Vite)
# =======================================
FROM node:18-alpine as builder

WORKDIR /app

# Copy dependency definitions
COPY package.json package-lock.json ./

# Install dependencies
# We use --legacy-peer-deps to avoid conflicts with Framer Motion / Lucide
RUN npm install --legacy-peer-deps

# Copy the rest of your UI code
COPY . .

# Build the app using Vite
# This will create a 'dist' folder (NOT 'build')
RUN npm run build

# =======================================
# Stage 2: Serve with Nginx
# =======================================
FROM nginx:alpine

# Remove default Nginx website
RUN rm -rf /usr/share/nginx/html/*

# COPY THE BUILD OUTPUT
# ---------------------------------------------------------------------------
# CRITICAL FIX FOR VITE:
# We copy from /app/dist instead of /app/build
# ---------------------------------------------------------------------------
COPY --from=builder /app/dist /usr/share/nginx/html

# Copy our custom Nginx config
COPY nginx/nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80 (Standard Web Port)
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]
